[build]
    command = "pnpm install --no-frozen-lockfile && pnpm run build"
    publish = "dist"

[dev]
    command = "npm run dev"
    targetPort = 3000

[[redirects]]
	from = "/*"
	to = "index.html"
	status = 200
